// PDF export removed — stub implementation
export async function exportToPDF() {
  throw new Error(
    "PDF export has been removed from this build. Remove calls to exportToPDF or re-enable the PDF feature."
  );
}
